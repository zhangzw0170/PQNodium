name: Audit

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo install cargo-audit
      - run: |
          cargo audit \
            # hickory-proto via libp2p — no fixed upgrade available (alpha dependency)
            --ignore RUSTSEC-2026-0118 \
            --ignore RUSTSEC-2026-0119 \
            # gtk-rs GTK3 bindings unmaintained — transitive via Tauri (Linux webview)
            --ignore RUSTSEC-2024-0370 \
            --ignore RUSTSEC-2024-0411 \
            --ignore RUSTSEC-2024-0412 \
            --ignore RUSTSEC-2024-0413 \
            --ignore RUSTSEC-2024-0414 \
            --ignore RUSTSEC-2024-0415 \
            --ignore RUSTSEC-2024-0416 \
            --ignore RUSTSEC-2024-0417 \
            --ignore RUSTSEC-2024-0418 \
            --ignore RUSTSEC-2024-0419 \
            --ignore RUSTSEC-2024-0420 \
            # glib VariantStrIter unsound — transitive via Tauri (Linux webview)
            --ignore RUSTSEC-2024-0429 \
            # pango unmaintained — transitive via Tauri (Linux webview)
            --ignore RUSTSEC-2024-0436 \
            # unic-* crates unmaintained — transitive via Tauri urlpattern
            --ignore RUSTSEC-2025-0075 \
            --ignore RUSTSEC-2025-0080 \
            --ignore RUSTSEC-2025-0081 \
            --ignore RUSTSEC-2025-0098 \
            --ignore RUSTSEC-2025-0100
